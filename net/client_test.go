package net

import (
	// "bytes"
	// "math/rand"
	"testing"
	// "time"
)

func TestSend(t *testing.T) {
	// InitServer()
	/*buf := make([]byte, 0, 128)
	buffer := bytes.NewBuffer(buf)
	go func() {
		for i := 0; i < 1; i++ {
			for j := 0; j < 3; j++ {
				buffer.Write([]byte{'a', 'b', 'c', 'd', 'e', 'f', 'g'})
				// buffer.WriteByte('a')
			}
			conn := InitClient()
			for j := 0; j < 9; j++ {
				Send(conn, buffer.Bytes())
			}
			Close(conn)
			buffer.Reset()
		}
	}()
	time.Sleep(time.Second * 99)*/
}
